# Domain Documentation

This repository uses a single bounded context.

- Read `CONTEXT.md` before work that changes domain behavior or vocabulary.
- Keep `CONTEXT.md` limited to canonical domain terms and definitions, without implementation details.
- Store system-wide architectural decisions in `docs/adr/`, creating the directory only when an ADR is warranted.
- Update the glossary when domain terminology changes.
