# Issue Tracker

Issues are tracked in GitHub Issues for the repository configured by the `origin` remote.

Engineering skills should use the GitHub CLI to read, create, update, label, and link issues. Pull requests are not an issue-request surface.
